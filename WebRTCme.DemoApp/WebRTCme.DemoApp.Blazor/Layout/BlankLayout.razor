@inherits LayoutComponentBase

@*
    No chrome: the call and chat pages want the whole viewport. They still need the theme's
    surface colour, which MudLayout would otherwise be providing.
*@
<div class="blank-layout">
    @Body
</div>
