<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
		<!-- Packed into WebRTCme or WebRTCme.Middleware, never shipped as a package of its own. -->
		<IsPackable>false</IsPackable>
    <TargetFrameworks>net10.0</TargetFrameworks>
  </PropertyGroup>

	<ItemGroup>
    <PackageReference Include="Utilme.Result" Version="1.0.1" />
    <PackageReference Include="Utilme.SdpTransform" Version="1.0.0" />
		<PackageReference Include="System.Reactive.Core" Version="6.1.0" />
		<PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" Version="10.0.9" />
	</ItemGroup>


  <ItemGroup>
    <ProjectReference Include="..\MediaSoup\WebRTCme.Connection.MediaSoup\WebRTCme.Connection.MediaSoup.csproj" />
    <ProjectReference Include="..\Signaling\WebRTCme.Connection.Signaling.Proxy\WebRTCme.Connection.Signaling.Proxy.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      SpeakingDetector is internal - it is the voice-activity rule, not surface anyone consumes -
      but it is a pure decision that took three attempts and a comma-decimal culture bug to get
      right, so it is exactly what unit tests are for.
    -->
    <InternalsVisibleTo Include="WebRTCme.Tests" />
  </ItemGroup>

</Project>
